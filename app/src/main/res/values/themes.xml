<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TipTime" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TipTime" parent="Base.Theme.TipTime" />

    <style name="Widget.MaterialComponents.ExtendedFloatingActionButton.ButtonCalculate">
        <item name="android:layout_marginTop">10dp</item>
        <item name="cornerRadius">30dp</item>
        <item name="backgroundTint">#136516</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.AppCompat.Switch">
        <item name="thumbTint">@color/green</item>
        <item name="trackTint">@color/teal_700</item>
    </style>
    <style name="Theme.AppCompat.Buttons">
        <item name="buttonTint">@color/green</item>
    </style>
</resources>